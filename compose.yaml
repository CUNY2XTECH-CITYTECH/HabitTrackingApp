services:

  postgres:
    image: postgres
    ports: 
      - '5432:5432'
    environment:
      - POSTGRES_PASSWORD=4484

  supertokens:
    image: registry.supertokens.io/supertokens/supertokens-postgresql